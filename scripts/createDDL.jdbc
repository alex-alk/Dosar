CREATE TABLE users (ID NUMBER(10) NOT NULL, PASSWORD VARCHAR2(100) NULL, USERNAME VARCHAR2(100) NULL, PRIMARY KEY (ID))
CREATE TABLE chapters (ID NUMBER(10) NOT NULL, CONTENT CLOB NULL, NAME VARCHAR2(255) NULL, PLATFORM_ID NUMBER(10) NULL, PRIMARY KEY (ID))
CREATE TABLE platforms (ID NUMBER(10) NOT NULL, NAME VARCHAR2(100) NULL, PRIMARY KEY (ID))
ALTER TABLE chapters ADD CONSTRAINT FK_chapters_PLATFORM_ID FOREIGN KEY (PLATFORM_ID) REFERENCES platforms (ID)
CREATE SEQUENCE SEQ_GEN_IDENTITY START WITH 1
CREATE SEQUENCE SEQ_GEN_IDENTITY START WITH 1
